# React 19 is ahead of some libraries' declared peer ranges (e.g.
# @tanstack/react-query@5.28.6 declares react ^18). The app works with React 19;
# allow npm to resolve these pre-existing peer mismatches instead of failing.
legacy-peer-deps=true
