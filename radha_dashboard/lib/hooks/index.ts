/**
 * lib/hooks — shared React hooks (useSession, useStoreScope, useDateRange, etc.).
 * Populated in Phase 03–05.
 */
export {
  useRegionLoadGuard,
  DEFAULT_REGION_TIMEOUT_MS,
} from './use-region-load-guard';
export type { RegionLoadGuardInput } from './use-region-load-guard';
