# 01 · Global — Brand, Icons, Backgrounds, Illustration DNA

> Paste `00_PRIMER.md`'s **LOCKED block** first. Then run these **in order** — they define RADHA's
> visual DNA, so every screen after stays consistent. Each fenced block is copy-paste ready.
> One generation per block; save to the path noted.

---

## B1 · Logo & wordmark  → `assets/logo/`
```
RADHA brand identity, per the RADHA locked system. Produce: (1) the WORDMARK "RADHA" in a warm
Plus-Jakarta-Sans-style w800, ink #1C1917, with a single small burnt-orange #EA580C spark/dot
integrated into one letter (not a separate icon); (2) a compact standalone BRAND MARK — an abstract
striped storefront-awning glyph (or a confident "R" monogram) that reads at tiny sizes. Clean,
premium, balanced, on a plain #FFFBF5 background, centered with even margins. No tagline, no coins,
no rewards motifs. Give the wordmark and the mark side by side.
Save → assets/logo/radha-wordmark.png  and  assets/logo/radha-mark.png
```

## B2 · App launcher icon  → `assets/logo/app-icon.png`
```
RADHA app launcher icon, per the locked system. The RADHA storefront-awning brand mark centered on a
warm burnt-orange #EA580C rounded-square, soft depth, a subtle cream highlight, and a ≤8% bandhani-dot
texture in one corner; crisp and legible at small sizes. Also give a second variant: cream mark on a
deeper #9A3412 square. 1024×1024, no text.
Save → assets/logo/app-icon.png
```

## B3 · Design-system reference board (the consistency anchor)  → `assets/other/radha-design-tokens-v2.png`
```
A single "RADHA design system reference board" (NOT a phone mockup), on warm cream #FFFBF5, neatly
laid out and labelled: (a) palette swatches with hex — #EA580C, #9A3412, #FED7AA, #1C1917, #57534E,
#FFFBF5, #FFFFFF, #F5F1E8, #E7E1D4, #15803D, #B45309, #B91C1C, #6D5BD0, #0F766E, festive #F59E0B/#FACC15;
(b) a type specimen (Plus Jakarta Sans headings + JetBrains Mono numerals 0–9, EAN, a date); (c) a row
of core components — a KPI tile, the orange Hero Story Banner, a segmented control with sliding orange
pill, a status pill, a product row, the 5-tab bottom nav; (d) an icon-family weight sample. Editorial,
premium, well-spaced. This board is the consistency anchor for all later images. 1536×1024.
Save → assets/other/radha-design-tokens-v2.png
```

## B4 · Bottom-nav icon set (5, inactive + active)  → `assets/icon/nav-set.png`
```
One icon sheet, two rows × five columns, per the locked icon family. TOP row INACTIVE (ink-soft
#57534E line); BOTTOM row ACTIVE (burnt-orange #EA580C filled-accent). Glyphs L→R: home, scan
(barcode viewfinder), expiry (clock/calendar), tasks (clipboard-check), profile (person). Single
~1.75px stroke, ~2px corner radius, rounded terminals, identical optical size, warm character; the
scan glyph slightly emphasized as the center action. On #FFFBF5, evenly spaced, labelled. No
Material/Lucide look.
Save → assets/icon/nav-set.png  (then trace each to SVG)
```

## B5 · Quick-action icon set (6)  → `assets/icon/quick-actions-set.png`
```
One icon sheet of 6 matching glyphs (same family as nav), each sitting in a soft accent-tint #FED7AA
rounded well: scan barcode, add expiry (calendar+plus), add task (clipboard+plus), create GRN
(delivery truck / inbox + doc), recall check (alert shield), stock count (stacked boxes / tally).
Ink-soft #57534E line glyphs, ~1.75px stroke, ~2px radius, rounded terminals, warm. On #FFFBF5.
Save → assets/icon/quick-actions-set.png
```

## B6 · Category glyphs (10) + product-cutout set (8)  → `assets/icon/` & `assets/illustration/products/`
```
(a) One icon sheet of 10 flat warm category glyphs (same family): biscuits & snacks, breakfast &
spreads, dairy & eggs, beverages, personal care, household, staples/grains, frozen, baby, health.
Ink-soft line with an optional matching category-tint fill. On #FFFBF5, labelled grid.
Save → assets/icon/category-set.png
```
```
(b) Clean real-product CUTOUTS on PURE WHITE, one consistent soft lighting, centered with even
padding (product never touches the edge), premium, with a soft contact shadow. Generate ONE at a
time: a biscuit pack · a spread jar · a milk carton with eggs · a cola + a juice bottle · a shampoo +
soap · a detergent bottle · a rice/atta pack · a frozen-peas pack. 1024×1024 each.
Save → assets/illustration/products/cat-biscuits.png, cat-dairy.png, cat-beverages.png, … 
```

## B7 · KPI / status icon set (6, tinted)  → `assets/icon/kpi-status-set.png`
```
One icon sheet of 6 glyphs (same family), each shown in its domain tint: expiring = clock (amber
#B45309), low-stock = box (indigo-violet #6D5BD0, flat not neon), tasks-due = clipboard (orange
#EA580C), GRN-pending = delivery truck (green #15803D), matched = check-seal (success #15803D),
store-health = shield-check (teal #0F766E). ~1.75px stroke, ~2px radius, rounded terminals. On
#FFFBF5, labelled.
Save → assets/icon/kpi-status-set.png
```

## B8 · Utility icon set  → `assets/icon/utility-set.png`
```
One icon sheet of utility glyphs (same family, ink-soft #57534E line, ~1.75px, ~2px radius, rounded
terminals): search, filter, sort, chevron-right, chevron-left, overflow (vertical dots), share, edit,
delete, back-arrow, close, calendar, camera/torch, info, alert-triangle, copy, WhatsApp. On #FFFBF5,
neat labelled grid.
Save → assets/icon/utility-set.png
```

## B9 · Backgrounds & textures (3)  → `assets/background/`
```
Three subtle, premium, text-safe background treatments on warm cream #FFFBF5, generated as a labelled
set of three panels: (1) a very faint warm paper grain (~3%) for the app canvas; (2) a soft hero wash
fading cream → #FFF3E6 top-to-bottom for header bands; (3) a ≤8% accent-tint #FED7AA bandhani-dot /
marigold-petal seam strip. Never busy, never overpowering. 1536×1024.
Save → assets/background/cream-grain.png, hero-wash.png, festive-seam.png
```

## B10 · Illustration DNA + core spot illustrations  → `assets/illustration/`
```
First, a small "RADHA illustration language" sample establishing the warm semi-flat 3D-lite style
(muted orange/cream/terracotta, soft depth, NOT glossy, friendly, premium). Then four spot
illustrations in exactly that style: (1) a low-poly Indian dukaan STOREFRONT with a striped RADHA
awning, neat parcels, and a warm skyline, composed top-right with the LEFT THIRD kept clear for text;
(2) a CALENDAR with an alert BELL (amber family) for expiry; (3) a SHIELD-CHECK with a small bar trend
(teal family) for store health; (4) an EMPTY-STATE moment — a tidy empty shelf with a magnifier and a
small friendly spark. On #FFFBF5.
Save → assets/illustration/storefront.png, spot-expiry.png, spot-storehealth.png, empty-scan.png
```

---

**Next:** once these exist, generate the screen packs (start with `08_home.md`) in the SAME chat so
the screens inherit this exact identity.
