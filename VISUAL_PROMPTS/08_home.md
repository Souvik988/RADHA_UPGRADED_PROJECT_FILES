# 08 · Home (Store Command) — ChatGPT prompts

> Full spec: `VISUAL_SCREENS/08_home.md`. Run these in the SAME ChatGPT chat as `00_PRIMER.md` +
> `01_brand_icons_backgrounds.md` so Home inherits the locked identity and the icons/illustrations
> you already made. One generation per block.

---

## H0 · Home — DEFAULT (the flagship)  → `assets/mockup/home.png`
```
RADHA — Home screen ("Store Command", business mode), DEFAULT state, full screen inside the clean
iPhone 15 mockup, per the RADHA locked system. Lay it out top → bottom, content-heavy but breathing,
with ONE orange focal point (the story banner):
• HERO BAND on a cream→#FFF3E6 wash with the low-poly RADHA storefront illustration top-right: a
  store-picker pill "Priya Store ▾" top-left; a notification bell with an orange unread dot + a round
  avatar top-right; greeting "Hi Priya! 👋" in w800 ink #1C1917; sub "Manage your store. Stay ahead."
  in ink-soft; a pill search bar "Search products, tasks, GRNs…" with a scan glyph, and a small
  Veg/Non-veg filter.
• KPI BENTO — a 2×2 grid of raised white tiles, each a small tinted glyph well + a big JetBrains-Mono
  number + label + a tiny "Action needed ›": "Expiring soon 18 items" (amber #B45309), "Low stock 7
  items" (violet #6D5BD0), "Tasks due 5 today" (orange #EA580C), "GRNs pending 2 to review" (green
  #15803D).
• HERO STORY BANNER — a wide burnt-orange #EA580C card: on the RIGHT a 3D-lite lit-shelf / audit
  cluster on a soft warm spotlight halo with a ≤8% marigold-string motif along the top edge; on the
  LEFT an eyebrow "AAJ KA KAAM · TODAY'S MISSION", a white 2-line headline "18 items expire before
  Friday — clear the shelf", a JetBrains-Mono progress ribbon "12 of 18 done" with a filling bar, and
  a white "Open expiry" pill. (Absolutely no rewards/coins/scan-to-earn.)
• QUICK ACTIONS — a row of 6 soft accent-tint icon tiles: Scan, Add expiry, Add task, Create GRN,
  Recall check, Stock count.
• TOP CATEGORIES — a horizontal rail of clean product cutouts with labels (Biscuits & Snacks, Dairy &
  Eggs, Beverages, Personal Care, …).
• Two side-by-side info cards: "Stay ahead of expiry dates" (calendar+bell spot, orange "Add expiry")
  and "Store health at a glance" (shield-check gauge, green "View dashboard").
• RECENTLY SCANNED — 3 rows: product cutout + name + JetBrains-Mono "EXP …" + status dot — Good Day
  Cashew 200g (green), Amul Butter 500g (green), Pepsi 2.25L (red).
• 5-tab BOTTOM NAV — Home active (orange), Scan center-emphasized, Expiry, Tasks, Profile.
Warm, premium, merchandised, readable. Portrait 1024×1536.
Save → assets/mockup/home.png
```

## H-mission · Hero Story Banner scene (illustration only)  → `assets/illustration/home-mission.png`
```
RADHA Home story-banner illustration (NO phone frame). A cinematic, merchandised "today's mission"
scene meant to sit on the RIGHT ~44% of the burnt-orange #EA580C banner: a 3D-lite lit shop shelf with
neat products + an audit clipboard + a couple of soft floating accent shapes, on a warm spotlight halo
#FFF3E6, muted (not glossy), edges that blend on orange, with a ≤8% marigold-string motif along the
top. Keep the left side clean for text. No gift/coin/reward motifs. 1536×1024, transparent or orange
background.
Save → assets/illustration/home-mission.png
```

## H1 · Home — LOADING  → `assets/mockup/home-loading.png`
```
RADHA Home — LOADING state, same screen and layout, but every zone is a soft sunken #F5F1E8 skeleton
block (hero greeting lines + chip, four KPI tiles, one wide banner block, six quick-action wells, a
category-well row, two info cards, three list rows), gentle shimmer implied; the bottom nav stays
solid. Warm, calm, never an empty white screen. Portrait 1024×1536.
Save → assets/mockup/home-loading.png
```

## H2 · Home — EMPTY recently-scanned  → `assets/mockup/home-empty.png`
```
RADHA Home — same screen, but the "Recently scanned" section is a designed EMPTY STATE with
personality: a tonal scan-glyph badge, "No scans yet" (w700 ink), "Scan your first product to see it
here" (ink-soft), and an orange "Scan now" pill. Everything above (hero, KPIs, banner, quick actions,
categories) renders normally. Portrait 1024×1536.
Save → assets/mockup/home-empty.png
```

## H3 · Home — CONSUMER variant  → `assets/mockup/home-consumer.png`
```
RADHA Home — CONSUMER variant of the same system: greeting + consumer KPIs (Today's scans / Saved /
Expiring), a consumer Hero Story Banner promoting real value (premium or family sharing — still one
orange banner, still no rewards), consumer quick actions (Scan, Add expiry, Allergens, Saved), a
categories rail, and recently-scanned rows. Slightly lighter and warmer than business mode, same
identity. Portrait 1024×1536.
Save → assets/mockup/home-consumer.png
```

## H4 · Home — FESTIVE (Diwali) skin  → `assets/mockup/home-festive.png`
```
RADHA Home — FESTIVE (Diwali) skin of the same screen: tokens and layout unchanged, but accents shift
toward marigold #F59E0B / turmeric #FACC15, with a tasteful diya / string-light micro-motif ONLY on
the story banner plus a subtle warm glow. Everything else stays identical, readable, and premium — a
seasonal chapter, not a redesign. Portrait 1024×1536.
Save → assets/mockup/home-festive.png
```

---

**Tip:** generate H0 first, confirm the identity matches your icons/board, then H-mission and the
state variants. When you start the next screen pack, keep this chat open so Scan/Expiry/etc. stay
visually consistent with Home.
